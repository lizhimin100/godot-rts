# 类魔兽 — 项目结构与分析

## 基本信息
- **引擎**: Godot 4.7 (GL Compatibility 渲染模式)
- **分辨率**: 1280×720 视口, 1300×800 窗口
- **插件**: Dialogic (对话系统), godot_mcp (MCP 集成)
- **类型**: 自走棋 + RTS 元素单机游戏

## 目录结构总览

```
类魔兽/
├── project.godot                    # 引擎配置
├── addons/
│   ├── dialogic/                    # Dialogic 对话插件
│   └── godot_mcp/                   # Godot MCP 运行时
│
├── 代码/                            # 所有 GDScript
│   ├── 自动加载/                     # Autoload 脚本
│   │   └── 全局变量.gd              # 全局状态（金钱、木材、食物等）
│   ├── 场景/                        # 主场景逻辑
│   │   ├── 面板.gd                  # 主菜单
│   │   ├── 营地场景.gd              # 营地（自走棋核心）
│   │   ├── 酒馆场景.gd              # 酒馆（商店/招募）
│   │   ├── 战斗场景.gd              # 战斗
│   │   └── 世界岛场景.gd            # 世界地图
│   ├── UI/                          # UI 脚本
│   │   └── 商店.gd                  # 商店逻辑
│   ├── 单位/                        # 单位系统
│   │   ├── 单位卡/                   # 单位卡牌 UI
│   │   └── 单位等级/                 # 单位升级
│   ├── 卡牌/                        # 卡牌战斗系统
│   │   ├── 卡牌UI.gd
│   │   └── 卡牌状态/                 # 状态机（BASE/CLICKED/DRAGGING/RELEASED）
│   ├── 属性/                        # 属性系统
│   │   ├── Attribute.gd            # 属性基类
│   │   ├── AttributeModifler.gd    # 属性修饰器
│   │   └── AttributeSet.gd         # 属性集合
│   ├── 组件代码/                     # 可复用组件
│   │   ├── 单位网格组件.gd          # 网格管理
│   │   ├── 自走棋单位移动组件.gd     # 单位移动
│   │   ├── 单位组合器组件.gd         # 单位合成（三合一）
│   │   ├── 游戏瓦片层组件.gd         # 瓦片坐标转换
│   │   ├── 卡牌状态机组件.gd         # 卡牌状态机
│   │   └── 拿起_和_放下.gd           # 拖放组件
│   ├── 攻击/                        # 战斗属性
│   │   └── 血量属性.gd              # HP 系统
│   ├── 角色基础/                     # 单位基类
│   │   └── 移动基类.gd              # 移动逻辑
│   ├── 拖放UI/                      # 拖放功能
│   ├── 框选和点选控制.gd            # RTS 框选/点选系统
│   ├── 战争迷雾.gd                  # 战争迷雾（shader 实现）
│   ├── 选择ui.gd                    # 选择指示器 UI
│   └── 自定义父类与子类/             # 基类扩展
│       ├── 对话互动.gd              # 对话交互
│       └── 资源互动.gd              # 资源采集
│
├── 场景/                            # 场景文件 (.tscn)
│   ├── 面板.tscn                    # 主菜单（主场景）
│   ├── 营地场景.tscn                # 营地场景
│   ├── 酒馆场景.tscn                # 酒馆场景
│   ├── 战斗场景.tscn                # 战斗场景
│   ├── 世界岛场景.tscn              # 世界地图
│   ├── 出售入口.tscn                # 出售单位入口
│   ├── 框选和点选界面.tscn
│   └── scenes(场景)/                # 备用场景
│
├── 单位/                            # 单位资源
│   ├── 单位数据/                     # UnitStats .tres 资源
│   ├── 单位池/                       # UnitPool .tres 资源
│   ├── 单位卡/                       # 单位卡牌场景
│   ├── 单位基础/                     # 单位基础场景
│   ├── 单位拖放/                     # 单位拖放场景
│   └── 单位等级相关/                 # 等级相关
│
├── UI/                              # UI 场景
│   ├── 侧部弹出标签/
│   ├── 存储UI/
│   └── 建造ui/
│
├── UI组件/                          # UI 组件场景
│   ├── 商店UI组件/
│   ├── 总UI/                        # 资源和经验UI
│   ├── 战斗UI/
│   └── 酒馆UI组件/
│
├── 玩家/                            # 玩家状态资源
│   └── 玩家状态.gd
│
├── 物品系统/                        # 物品和背包
│   ├── 物品/                        # 物品数据
│   └── 背包/
│
├── 自动加载/                        # Autoload 场景
│   └── 防建筑重叠.tscn
│
├── 状态机/                          # 状态机系统
│
├── 角色/                            # 角色数据/对话
├── 对话文件/                        # Dialogic 对话文件
│
├── assets/                          # 资产（fonts, music, sfx, sprites, shaders）
├── 美术、音频/                       # 额外美术/音频
├── 小剑资源/                        # 精灵包
├── 忍者地形/                        # 地形贴图
├── 测试/                            # 测试场景/脚本
└── 主题/                            # Theme .tres 资源
```

## 核心系统架构

| 系统 | 位置 | 说明 |
|------|------|------|
| 全局变量 | `代码/自动加载/全局变量.gd` | Autoload，管理金币/木材/食物/选择/命令状态 |
| 单位网格 | `代码/组件代码/单位网格组件.gd` | 基于网格的单位管理，含信号 |
| 单位合成 | `代码/组件代码/单位组合器组件.gd` | 三合成一，升级 |
| 属性系统 | `代码/属性/` | Attribute + Modifier + Set |
| 卡牌状态机 | `代码/卡牌/卡牌状态/` | BASE→CLICKED→DRAGGING→RELEASED |
| RTS控制 | `代码/框选和点选控制.gd` | 框选/点选+移动命令 |
| 战争迷雾 | `代码/战争迷雾.gd` | Shader 实现 |
| 对话系统 | Dialogic 插件 | 支持分支对话 |

## 输入映射

| 动作 | 按键 |
|------|------|
| 选择 | 鼠标左键 |
| 命令/移动 | 鼠标右键 |
| 快速出售 | S |
| 取消 | 鼠标右键 |
| 交互 | H |
| 镜片缩放 | PageUp/PageDown |
| UI确认 | Enter |

## 物理层

1. card_target_selector
2. card_drop_area
3. 建筑
4. 操作单位（己方）
5. 敌人单位
6. 环境
7. 资源

## 已修复的代码错误

| 文件 | 问题 | 修复 |
|------|------|------|
| `card_dragging_state.gd:17` | `get_global_mouse_positon()` 拼写错误 | 改为 `get_global_mouse_position()` |
| `card_released_state.gd:3` | `func ented()` 方法名错误 | 改为 `func enter()` |
| `选择ui.gd:6,9` | `visble` 拼写错误 | 改为 `visible` |
| `单位网格组件.gd:38` | `unit_grid_changed` 缺少 emit() | 改为 `unit_grid_changed.emit()` |
| `血量属性.gd` | setter 中 `health` 自引用潜在递归 | 使用分离的 `_血量` 后备变量 |
| `资源互动.gd:9` | 悬空字符串字面量 | 改为 `preload()` 赋值给变量 |
| `场景/平面.tscn:98` | `.tscn` 中 `Color(1, 1, 1)` 使用 RGB 3 参数，Godot 4.7 文本格式解析器要求 RGBA 4 参数 | 改为 `Color(1, 1, 1, 1)` |

## Godot 4.7 技术坑记录

### .tscn 中 Color 必须用 4 参数 RGBA
- **现象**：Godot 4.7 在加载 `.tscn` 文件时，若 `Color` 值使用 3 参数（如 `Color(1, 1, 1)`），会报 "Parse error" 且无法加载场景。
- **原因**：Godot 4.7 的文本格式解析器 `resource_format_text.cpp` 在解析节点属性时，要求 `Color` 必须显式提供全部 4 个分量 (R, G, B, A)。3 参数写法虽然 **在 GDScript 运行时** 是合法的（会自动补 A=1.0），**但在 .tscn 文本解析阶段** 不被接受。
- **影响范围**：所有 `.tscn` / `.tres` 文件中 Label 的 `theme_override_colors/font_color` 以及其他 Color 属性。
- **修复方法**：`Color(1, 1, 1)` → `Color(1, 1, 1, 1)`
- **预防措施**：在 `.tscn` 文件中写 `Color` 时始终使用 4 参数；在 GDScript 中写 `Color` 可以使用 3 参数（运行时会正确补全 alpha）。
