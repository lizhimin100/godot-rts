你正在重构一个Godot 4.7 RTS游戏的移动系统。

核心要求：

1. 禁止一次性重构
2. 必须按阶段执行
3. 每一阶段必须保持游戏可运行
4. 不允许删除现有系统（只能逐步替换）
5. 不允许多个系统同时拥有velocity控制权
6. 任何时候必须保证：
   - 单位仍然可以移动
   - 不允许出现“全局冻结”

核心目标：
构建 RTS Movement 2.0 架构：
MovementSolver 为唯一速度决策层